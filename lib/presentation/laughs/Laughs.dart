import 'package:flutter/material.dart';

class LaughsPage extends StatelessWidget {
  const LaughsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Laughs'),
      ),
    );
  }
}
